.env
*.db
positions.yaml
logs/*.log
reports/*.md
reports/*.html
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ruff_cache/
.mypy_cache/
.venv/
venv/
.idea/
.vscode/
*.egg-info/
dist/
build/

# D-062 G-04: root temp/build artifacts
PHASE_4_2_*_STATUS.txt
phase_4_2_*_output.txt
d_033_validation*.txt
test_output.txt
test_results*.txt
backtest_output.log
chat.html
vader_accuracy_results.txt

# D-116 Fintables takas scraper (cookie + custody DB; *.db zaten ignore'da)
.fintables_session.json
data/custody/

# D-120 operational reliability (logs/*.log alt dizinleri kapsamıyor)
logs/failures/
data/backups/

# D-139 IC framework: personal signal data + IC time-series (public repo leak guard)
data/analytics/
data/signal_logs/
data/event_logs/

# Runtime / generated / local junk (not code)
agents/intelligence/
reports/
decisions/
.efficiency/
%null%
.claude/settings.local.json
test_out.txt
requirements_full.txt
cache/
kap_*.json
claude kritikler.md
invalid/

*_session.json

# Public-release: keep local, hide from GitHub (preserve-local cleanup)
# _governance/ = critical project-continuity / AI-memory (CRITIC_BACKLOG, MASTER_PLAN, orchestrator/boot docs)
# miscellaneous/ = transient dev scratch / deprecated notes
# OS_STATE.md, masterplan.md, CRITIC_BACKLOG.md = code/test-read -> hidden in place (not moved)
_governance/
miscellaneous/
OS_STATE.md
agents/memory/masterplan.md
CRITIC_BACKLOG.md
# tests private CRITIC_BACKLOG.md/OS_STATE.md (local-only); inline comment kaldirildi (pattern'i bozuyordu)
tests/test_critic_backlog.py

# D-143 NAV Discount Tracker: local holdings config (never commit)
config/

# Runtime-generated cache files (yanlislikla D-154'te commit edildi — D-154 hotfix)
data/news_cache.json
data/sentiment_cache.json

# Obsidian editor config (personal, not tracked)
.obsidian/

# Builder PR/commit artifacts (local workflow helpers; organized in pr-artifacts/)
pr-artifacts/
data/*.local.bak

# D-199 archive: ham payload'lari ignore et, _manifest.json commit edilir
data/bist_datastore_archive/**/*.zip
data/bist_datastore_archive/**/*.csv
data/bist_datastore_archive/**/*.xls
data/bist_datastore_archive/**/*.xlsx

# D-200 clean universe: ham cikti gitignore, _meta.json commit edilir
data/clean_universe/*.parquet
