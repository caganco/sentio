## D-150d: tests/test_backtest_statistical.py — 19 unit tests

SPEC_STATISTICAL_VALIDATION_1 §5 implementasyonu — D-150 serisinin dördüncü adımı.

### Değişiklikler

**Yeni:** `tests/test_backtest_statistical.py`

**Test Dağılımı (19 test):**

| Sınıf | Testler | Kapsam |
|-------|---------|--------|
| `TestPurgedKFold` (4) | split_yields_n_splits, purge_removes_samples, embargo_removes_samples, no_overlap | Purge/embargo zone + ∩=∅ garantisi |
| `TestCombinatorialPurgedCV` (3) | n_paths_c_n_k, split_count, no_overlap | C(6,2)=15 + ∩=∅ garantisi |
| `TestDSR` (4) | high_sr, negative_sr, threshold_constant, fat_tail | DSR matematiksel doğrulama |
| `TestPBO` (3) | all_positive, all_negative, threshold_constant | PBO sınır değerleri |
| `TestMinBTL` (3) | approx_307, monoton_artış, constant_553 | Parametrik form + RR-018 sabiti |
| `TestNeweyWest` (2) | autocorrelated, iid | HAC varyans artışı etkisi |

### SPEC §5'ten Sapma Notları (2 adet)

**Sapma 1 — fat tail test parametresi (direktif zorunlu):**
- SPEC: `[1.5]*15, T=250` — T=250 ile norm.cdf satürasyona girer (DSR≈1.0 her iki kurtosis için)
- D-150d: `[0.5]*15, T=15` — norm.cdf(1.764)=0.961 vs norm.cdf(1.634)=0.949 → fark ölçülür ✓

**Sapma 2 — TestNeweyWest baseline (matematiksel zorunluluk):**
- SPEC: `naive = calculate_sharpe(list(eq))` — rf_rate=0.42 (TCMB) çıkartır; NW Sharpe çıkartmaz; karşılaştırılamaz
- D-150d: `nw_iid = sharpe_newey_west(returns, lags=0)` — aynı formül, HAC olmadan; doğrudan karşılaştırılabilir
- Matematiksel kanıt: `autocorr[i] = 0.4*base[i-1] + 0.6*base[i]` → lag-1 cov = 0.24σ² > 0 → HAC var artışı → nw_hac < nw_iid garantili

### Doğrulama
- `pytest tests/test_backtest_statistical.py -v`: 19 passed, 0 failed, 0 skipped ✓
- `pytest tests/ -q`: 1351 passed, 4 skipped (sıfır regresyon) ✓
- Canlı API çağrısı yok — pure-math unit tests ✓
- BacktestEngine import edilmedi ✓
- import-linter: backtest→signals import yok ✓

### Bağımlılıklar
- **Önceki:** D-150c (PR #78, merged) — `statistical_validation.py`
- **Sonraki:** D-150e — Crisis integration + BACKTEST_SYSTEM.md Faz 2 + summary.json RETRACT

**Dayanak:** SPEC_STATISTICAL_VALIDATION_1 §5, RR-018 §§5–7

🤖 Generated with [Claude Code](https://claude.com/claude-code)
