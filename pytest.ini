[pytest]
addopts = --tb=short -q
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    baseline: Previously passing tests (do not re-run)
    new: New feature tests (always run)
    regression: Regression detection (weekly)
    integration: Integration tests (slow)
